## 1. Reference Pages — Simple Modal Forms

- [x] 1.1 Add `onKeyDown` Enter guard to the `<form>` in `carriers/page.tsx` Modal
- [x] 1.2 Add `onKeyDown` Enter guard to the `<form>` in `customers/page.tsx` Modal
- [x] 1.3 Add `onKeyDown` Enter guard to the `<form>` in `container-sizes/page.tsx` Modal
- [x] 1.4 Add `onKeyDown` Enter guard to the `<form>` in `cost-templates/page.tsx` Modal
- [x] 1.5 Add `onKeyDown` Enter guard to the `<form>` in `locations/page.tsx` Modal
- [x] 1.6 Add `onKeyDown` Enter guard to the `<form>` in `service-types/page.tsx` Modal
- [x] 1.7 Add `onKeyDown` Enter guard to the `<form>` in `users/page.tsx` Modal

## 2. Record & Move Pages

- [x] 2.1 Add `onKeyDown` Enter guard to the `<form>` in `vehicle-records/page.tsx` Modal
- [x] 2.2 Add `onKeyDown` Enter guard to the `<form>` in `vehicle-maintenance/page.tsx` EditModal
- [x] 2.3 Add `onKeyDown` Enter guard to the `<form>` in `yard-moves/page.tsx` Modal

## 3. Trip Plans (Complex Form)

- [x] 3.1 Add `onKeyDown` Enter guard to the `<form>` in `trip-plans/page.tsx` TripFormBody component

## 4. Login Page

- [x] 4.1 Add `onKeyDown` Enter guard to the `<form>` in `login/page.tsx`

## 5. Verification

- [ ] 5.1 Manually verify: open each modal, press Enter in a text input — form must NOT submit
- [ ] 5.2 Manually verify: submit button click still submits the form correctly on each page
- [ ] 5.3 Manually verify: Enter in textarea (if any) still inserts newline
- [x] 5.4 Run TypeScript type-check — `npm run type-check` in `apps/web`
