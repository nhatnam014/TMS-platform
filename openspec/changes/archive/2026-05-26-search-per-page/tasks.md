## 1. Drivers Page Search

- [x] 1.1 Add `search` state and `displayed` filtered array to drivers page (filter on `fullName` and `phone`)
- [x] 1.2 Render search input above the drivers table, wired to `search` state

## 2. Customers Page Search

- [x] 2.1 Add `search` state and `displayed` filtered array to customers page (filter on `code` and `name`)
- [x] 2.2 Render search input above the customers table, wired to `search` state

## 3. Carriers Page Search

- [x] 3.1 Add `search` state and `displayed` filtered array to carriers page (filter on `code` and `name`)
- [x] 3.2 Render search input above the carriers table, wired to `search` state

## 4. Locations Page Search

- [x] 4.1 Add `search` state and update existing `displayed` filter to include `code` and `name` (AND with existing type filter)
- [x] 4.2 Render search input above the locations table, wired to `search` state

## 5. Users Page Search

- [x] 5.1 Add `search` state and `displayed` filtered array to users page (filter on `username`)
- [x] 5.2 Render search input above the users table, wired to `search` state

## 6. Containers Page Search

- [x] 6.1 Add `search` state and `displayed` filtered array to containers page (filter on `containerNumber`)
- [x] 6.2 Render search input above the containers table, wired to `search` state
