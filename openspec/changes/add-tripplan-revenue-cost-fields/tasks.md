## 1. Database schema — packages/db/prisma/schema.prisma

- [x] 1.1 Add 7 new nullable columns to the `TripPlan` model, positioned near the existing 8 fixed cost slots: `luongAmount Decimal? @db.Decimal(15, 2) @map("luong_amount")`, `cuocAmount Decimal? @db.Decimal(15, 2) @map("cuoc_amount")`, `doanhThuAmount Decimal? @db.Decimal(15, 2) @map("doanh_thu_amount")`, `phuThuAmount Decimal? @db.Decimal(15, 2) @map("phu_thu_amount")`, `chiPhiAmount Decimal? @db.Decimal(15, 2) @map("chi_phi_amount")`, `tienDauAmount Decimal? @db.Decimal(15, 2) @map("tien_dau_amount")`, `neoXeAmount Decimal? @db.Decimal(15, 2) @map("neo_xe_amount")`
- [x] 1.2 Run `pnpm prisma migrate dev --name add_trip_plan_revenue_cost_fields` inside `packages/db`; confirm the generated SQL adds 7 nullable columns to `trip_plans` (additive, no data loss)
- [x] 1.3 Regenerate the Prisma client (`pnpm prisma generate` if not run automatically by `migrate dev`) and confirm `apps/api` type-checks against the 7 new fields

## 2. API — CreateTripPlanDto / UpdateTripPlanDto

- [x] 2.1 In `apps/api/src/modules/trip-plan/dto/create-trip-plan.dto.ts`, add 7 new optional fields after the existing fixed cost slot fields: `luongAmount`, `cuocAmount`, `doanhThuAmount`, `phuThuAmount`, `chiPhiAmount`, `tienDauAmount`, `neoXeAmount`, each decorated `@ApiPropertyOptional() @IsOptional() @IsNumber() @IsPositive()`
- [x] 2.2 Mirror the same 7 fields in `apps/api/src/modules/trip-plan/dto/update-trip-plan.dto.ts` (check whether it extends `CreateTripPlanDto` via `PartialType` or duplicates fields — follow whichever pattern the file already uses)
- [x] 2.3 Add the 7 fields to the shared `CreateTripPlanDto`/`UpdateTripPlanDto` TypeScript interfaces in `packages/shared/src/index.ts` if they declare cost-slot fields explicitly (check how `phiCuocAmount` etc. are declared there)

## 3. API — trip-plan.service.ts

- [x] 3.1 In `create()` (~line 146), add the 7 new fields to the `data` object passed to `tx.tripPlan.create`, following the existing `phiCuocAmount`/`phiCuocName` pattern (~line 192-193) but amount-only: `luongAmount: dto.luongAmount ?? null`, and likewise for the other 6
- [x] 3.2 In `update()` (~line 261), add conditional-spread updates for the 7 new fields, following the existing `...(dto.phiCuocAmount !== undefined && { phiCuocAmount: dto.phiCuocAmount })` pattern (~line 306)
- [x] 3.3 Confirm `findAll`'s response mapping and `TRIP_PLAN_INCLUDE`/select shape (if any explicit field selection exists) doesn't need changes — Prisma's default `findMany` already returns all scalar columns, but double-check no explicit `select` clause would silently drop the 7 new fields

## 4. Excel export — kehoach-xe.builder.ts

- [x] 4.1 Insert 7 new headers into the `HEADERS` array, immediately after `"CẦU ĐƯỜNG"` and before `"NGÀY GỬI CT"`: `"LƯƠNG"`, `"CƯỚC"`, `"DOANH THU"`, `"PHỤ THU"`, `"CHI PHÍ"`, `"TIỀN DẦU"`, `"NEO XE"`
- [x] 4.2 Insert 7 matching width values into `COL_WIDTHS` at the same array position (pick reasonable widths, e.g. 14-16, consistent with neighboring amount columns)
- [x] 4.3 Insert 7 matching values into the `dataRow` array passed to `ws.addRow(...)`, at the same position, immediately after `tp.cauDuongAmount != null ? Number(tp.cauDuongAmount) : ""` and before `formatDate(tp.documentSentDate)`: `tp.luongAmount != null ? Number(tp.luongAmount) : ""`, and likewise for the other 6 (`cuocAmount`, `doanhThuAmount`, `phuThuAmount`, `chiPhiAmount`, `tienDauAmount`, `neoXeAmount`)
- [x] 4.4 Verify the header block's `endCol`/merge-range logic (title/date merges at `mergeCells(3, 8, 3, endCol)` etc.) is unaffected — `endCol` is a fixed constant (15), not derived from `HEADERS.length`, so no change needed there

## 5. Excel import — kehoach-xe.parser.ts

- [x] 5.1 Add 7 new optional fields to `ParsedTripPlanRow`: `luongAmount?: number`, `cuocAmount?: number`, `doanhThuAmount?: number`, `phuThuAmount?: number`, `chiPhiAmount?: number`, `tienDauAmount?: number`, `neoXeAmount?: number`
- [x] 5.2 Add 7 new entries to `COLUMN_CANDIDATES`: `LUONG: { occurrence: 0, texts: ["LƯƠNG"] }`, `CUOC: { occurrence: 0, texts: ["CƯỚC"] }`, `DOANH_THU: { occurrence: 0, texts: ["DOANH THU"] }`, `PHU_THU: { occurrence: 0, texts: ["PHỤ THU"] }`, `CHI_PHI: { occurrence: 0, texts: ["CHI PHÍ"] }`, `TIEN_DAU: { occurrence: 0, texts: ["TIỀN DẦU"] }`, `NEO_XE: { occurrence: 0, texts: ["NEO XE"] }`
- [x] 5.3 In the row-building object returned by `parseKeHoachXe`, add `luongAmount: cellNum(row, COL.LUONG)`, and likewise for the other 6, using the same `cellNum()` helper already used for `phiCuocAmount` etc.
- [x] 5.4 Confirm `resolveColumns()`'s missing-column warning logic (`Không tìm thấy cột "<label>" trong file`) automatically covers the 7 new candidates with no additional code — verify by temporarily renaming one of the new headers in a test file and checking a warning is produced

## 6. Import service — import.service.ts (importTripPlans)

- [x] 6.1 In the CREATE branch's `tripPlanData` object, add the 7 new fields: `luongAmount: row.luongAmount ?? null`, and likewise for the other 6 (matching whatever pattern the existing 8 fixed-slot fields use in this branch)
- [x] 6.2 In the UPDATE branch's `tripPlanData` object, add conditional-spread entries for the 7 new fields: `...(row.luongAmount !== undefined && { luongAmount: row.luongAmount })`, and likewise for the other 6
- [x] 6.3 Confirm the "before" snapshot fetched for the changed-record diff (before the UPDATE branch's `tx.tripPlan.update` call) includes the 7 new fields — if the fetch uses an explicit `select`, add them; if it fetches the full row, no change needed
- [x] 6.4 Confirm the field-by-field diff loop picks up the 7 new fields automatically since it iterates over keys present in `tripPlanData` (no field-specific code needed) — verify with a manual test (see task 8.3)

## 7. Web — trip-plans/page.tsx: TripPlanRow type and list table

- [x] 7.1 Add 7 new fields to the `TripPlanRow` interface (~line 33): `luongAmount: number | null`, `cuocAmount: number | null`, `doanhThuAmount: number | null`, `phuThuAmount: number | null`, `chiPhiAmount: number | null`, `tienDauAmount: number | null`, `neoXeAmount: number | null`
- [x] 7.2 Add 7 new `<th>`/`<td>` column definitions to the list table, positioned immediately after the CẦU ĐƯỜNG column and before the NGÀY GỬI CT column, each rendering `fmt(trip.luongAmount)` (and the other 6) using the existing `fmt()` formatting helper, blank when null

## 8. Web — trip-plans/page.tsx: create/edit form cost section

- [x] 8.1 Add 7 new state fields to `CreateTripModal` (and the equivalent state/props in `EditTripModal`) for the new amount inputs — following whatever local-state pattern the component already uses for amount-only slots (check whether `phiCuocAmount` currently flows through the `slots` object with `hasShd: false`, and whether these 7 new fields need a separate lightweight state shape since they have no name/SHĐ, unlike `slots[key]`)
- [x] 8.2 Render 7 new cost cells in the "Chi phí chuyến" grid, immediately after the CẦU ĐƯỜNG `CostSlotInput`, each showing only a label (LƯƠNG, CƯỚC, DOANH THU, PHỤ THU, CHI PHÍ, TIỀN DẦU, NEO XE) and a single formatted-amount text input — reuse the existing amount-input styling/formatting helpers (`fmtInput`/`stripNonDigits`) but do NOT reuse `CostSlotInput` as-is if it always renders a name input; add a lighter-weight amount-only cell (new small component or inline JSX) instead
- [x] 8.3 Wire the 7 new inputs into the POST body on create (`toNum(...)` conversion, matching the existing pattern for `phiCuocAmount`) and the PUT body on edit
- [x] 8.4 Ensure `EditTripModal` pre-fills the 7 new inputs from the trip plan's existing values when opened

## 9. Verification

- [x] 9.1 `cd apps/api && pnpm exec tsc --noEmit` — confirm no type errors introduced
- [x] 9.2 `cd apps/web && pnpm exec tsc --noEmit` — confirm no type errors introduced
- [ ] 9.3 Manually create a trip plan via the "Tạo chuyến" form, filling in all 7 new fields, and confirm the created row's edit form shows the same values
- [x] 9.4 Manually export "kế hoạch xe" and confirm the 7 new columns appear in the correct position (after CẦU ĐƯỜNG, before NGÀY GỬI CT) with the correct values, and NỘI DUNG/GHI CHÚ/ID are still the last 3 columns — verified via an automated smoke script (`buildKeHoachXe()` → load buffer back with ExcelJS): header order and cell values confirmed programmatically; not visually opened in Excel/LibreOffice
- [ ] 9.5 Manually edit one of the 7 new columns in the exported file, re-import, and confirm the trip plan's edit form and a fresh export both show the updated value — requires running dev servers + browser, not exercised this session
- [ ] 9.6 Manually confirm the import result's changed-records section (from `fix-import-ke-hoach-xe-cost-fields`) shows the correct field/old/new values for an edited new-field column, and the Audit Log Viewer shows a matching entry — requires running dev servers + browser, not exercised this session
- [x] 9.7 Manually confirm leaving one of the 7 new fields blank on create/import correctly stores `null`, and blank cells on re-import do not clear an existing value — verified via a direct Prisma smoke script: partial update (only `phuThuAmount`) left `luongAmount`/`neoXeAmount` untouched
- [ ] 9.8 Manually confirm the trip plans list page shows the 7 new columns via horizontal scroll, correctly positioned and formatted — requires running dev servers + browser, not exercised this session
- [x] 9.9 Manually rename one of the 7 new column headers in an exported file (e.g. "CHI PHÍ" → something else) and re-import, confirming a "Không tìm thấy cột" warning is produced and that field is left unset for the import, rather than silently misreading another column — verified via smoke script: renaming "CHI PHÍ" produced the expected warning, `chiPhiAmount` came back `undefined`, and neighboring `chiPhiKhacAmount`/`chiPhiTraiTuyenAmount` were unaffected

## 10. Follow-up — add editable `*Name` and `shd*` fields to the 7 revenue/cost fields (2026-07-15 revision)

- [x] 10.1 In `packages/db/prisma/schema.prisma`, add 14 new nullable `String` columns to `TripPlan`, next to their existing `*Amount` counterparts: `luongName String? @map("luong_name")`, `shdLuong String? @map("shd_luong")`, `cuocName String? @map("cuoc_name")`, `shdCuoc String? @map("shd_cuoc")`, `doanhThuName String? @map("doanh_thu_name")`, `shdDoanhThu String? @map("shd_doanh_thu")`, `phuThuName String? @map("phu_thu_name")`, `shdPhuThu String? @map("shd_phu_thu")`, `chiPhiName String? @map("chi_phi_name")`, `shdChiPhi String? @map("shd_chi_phi")`, `tienDauName String? @map("tien_dau_name")`, `shdTienDau String? @map("shd_tien_dau")`, `neoXeName String? @map("neo_xe_name")`, `shdNeoXe String? @map("shd_neo_xe")`
- [x] 10.2 Run `pnpm prisma migrate dev --name add_trip_plan_revenue_cost_names_shd` inside `packages/db`; confirm the generated SQL adds 14 nullable `String` columns to `trip_plans` (additive, no data loss); regenerate the Prisma client — `migrate dev`'s shadow DB replay hit a pre-existing, unrelated bug in migration `20260624065625_add_km_hien_tai_ghi_chu_bao_duong` (a `name[] = text[]` comparison with no cast). Worked around it without touching that migration: generated the diff directly against the real dev DB via `prisma migrate diff --from-url ... --to-schema-datamodel ... --script` (confirmed it was exactly the 14 expected `ADD COLUMN` statements, nothing else), wrote it to a normal timestamped migration folder, applied via `prisma db execute`, and recorded it with `prisma migrate resolve --applied`. `prisma migrate status` now reports the schema up to date; Prisma client regenerated.
- [x] 10.3 In `apps/api/src/modules/trip-plan/dto/create-trip-plan.dto.ts`, add 14 new optional string fields next to the existing `*Amount` fields (~line 129-137): `@ApiPropertyOptional() @IsOptional() @IsString() luongName?: string;` / `shdLuong?: string;` and likewise for `cuoc`, `doanhThu`, `phuThu`, `chiPhi`, `tienDau`, `neoXe`, matching the `phiNangName`/`shdNang`-style declarations above them
- [x] 10.4 Mirror the same 14 fields in `update-trip-plan.dto.ts` (follow whichever pattern — `PartialType` vs duplicated fields — the file already uses) — file uses `PartialType(CreateTripPlanDto)`, so the 14 new fields are automatically inherited as optional; no edit needed
- [x] 10.5 Add the 14 `*Name`/`shd*` fields to the shared `CreateTripPlanDto`/`UpdateTripPlanDto`/`TripPlan` TypeScript interfaces in `packages/shared/src/index.ts`, next to where the 7 `*Amount` fields are already declared there
- [x] 10.6 In `trip-plan.service.ts`'s `create()`, add the 14 `*Name`/`shd*` fields to the `data` object passed to `tx.tripPlan.create`, following the existing `phiNangName: dto.phiNangName ?? null` / `shdNang: dto.shdNang ?? null` pattern
- [x] 10.7 In `trip-plan.service.ts`'s `update()`, add conditional-spread updates for the 14 `*Name`/`shd*` fields, following the existing `...(dto.phiNangName !== undefined && { phiNangName: dto.phiNangName })` pattern
- [x] 10.8 In `apps/web/src/app/(authenticated)/trip-plans/page.tsx`, add `luongName`/`shdLuong`, `cuocName`/`shdCuoc`, `doanhThuName`/`shdDoanhThu`, `phuThuName`/`shdPhuThu`, `chiPhiName`/`shdChiPhi`, `tienDauName`/`shdTienDau`, `neoXeName`/`shdNeoXe` (`string | null`) to the `TripPlanRow` interface
- [x] 10.9 Fold the 7 `EXTRA_COST_DEFAULTS` entries into `FIXED_SLOT_DEFAULTS` (or otherwise unify their state shape with the 8 existing slots), each with `hasShd: true`, so they can be driven by the existing `slots` state object and `slotsToPayload()` helper instead of the separate `extraCosts`/`extraCostsToPayload()` amount-only state
- [x] 10.10 Replace the 7 `ExtraCostInput` cells in the "Chi phí chuyến" grid with `CostSlotInput` (same component the 8 existing slots use), passing `costOptions={costTemplateOptions}` and `hasShd={true}`; remove `ExtraCostInput`/`EXTRA_COST_DEFAULTS`/`extraCosts` state if nothing else references them after the swap — `FIXED_SLOT_DEFAULTS.map(...)` already renders every slot via `CostSlotInput`, so folding the 7 new entries into that array (task 10.9) made the separate `ExtraCostInput`/`EXTRA_COST_DEFAULTS.map(...)` block redundant; deleted it along with the now-unused `ExtraCostInput` component, `ExtraCostsState` type, `emptyExtraCosts`/`initExtraCostsFromTrip`/`extraCostsToPayload` helpers, and `EXTRA_COST_BG`/`EXTRA_COST_COLUMNS` constants
- [x] 10.11 Wire the 14 new `*Name`/`shd*` values into the POST body on create and the PUT body on edit (via the unified `slotsToPayload()`), alongside the existing `*Amount` wiring
- [x] 10.12 Ensure `EditTripModal` pre-fills the 14 new `*Name`/`shd*` combobox/SHĐ values from the trip plan's existing values when opened, alongside the existing `*Amount` pre-fill
- [x] 10.13 Add 7 new SHĐ `<th>`/`<td>` column definitions to the list table, positioned immediately after each corresponding amount column, matching the existing 4-slot SHĐ display pattern — folded the 7 new fields into the existing `COST_COLUMNS` array (which already drives both header and row rendering with optional `shdLabel`/`shdKey`) instead of keeping the separate amount-only `EXTRA_COST_COLUMNS`; updated the empty-state `colSpan` from 38 to 45 (7 new SHĐ columns)
- [x] 10.14 In `apps/api/src/modules/export/builders/kehoach-xe.builder.ts`, insert 7 new "SHĐ &lt;FIELD&gt;" headers into `HEADERS`/`COL_WIDTHS`/the data row, each immediately after its corresponding amount column (e.g. `"SHĐ LƯƠNG"` right after `"LƯƠNG"`), and add `tp.shdLuong ?? ""` (and the other 6) to the data row
- [x] 10.15 In `apps/api/src/modules/import/parsers/kehoach-xe.parser.ts`, add 7 new `ParsedTripPlanRow` fields (`shdLuong?: string`, etc.) and 7 new `COLUMN_CANDIDATES` entries (`SHD_LUONG: { occurrence: 0, texts: ["SHĐ LƯƠNG"] }`, etc.), and populate them in the row-building object via `cellText(row, COL.SHD_LUONG) || undefined` (matching how `shdNang` etc. are read)
- [x] 10.16 In `apps/api/src/modules/import/import.service.ts`'s `importTripPlans`, add the 7 new `shd*` fields to both the CREATE and UPDATE branches' `tripPlanData` objects (matching the existing `shdNang`-style handling), so they're covered by the existing changed-record diff
- [x] 10.17 `cd apps/api && pnpm exec tsc --noEmit` — confirm no type errors introduced (required rebuilding `packages/shared` first — `apps/api` resolves `@tms/shared` to its built `dist/`, not `src/`, so shared-type edits need `pnpm --filter @tms/shared build` before they're visible)
- [x] 10.18 `cd apps/web && pnpm exec tsc --noEmit` — confirm no type errors introduced
- [ ] 10.19 Manually create a trip plan via the "Tạo chuyến" form: for each of the 7 new cells, select an existing `CostTemplate` option in one and type a free-text custom name in another, and fill in an SHĐ value; confirm the amount autofills on select, remains editable after free-typed entry, and `*Name`/`*Amount`/`shd*` values all persist correctly on the edit form and via `GET /trip-plans/:id`
- [ ] 10.20 Manually confirm leaving a new field's name/SHĐ blank while setting only the amount (and vice versa) works independently — blank name/amount/SHĐ stores `null` without affecting the others
- [ ] 10.21 Manually export "kế hoạch xe" and confirm each of the 7 new SHĐ columns appears immediately after its amount column with the correct value, and re-import confirms `shd*` fields round-trip through the changed-record diff same as `*Amount`

## 11. Follow-up — add TRẠNG THÁI (status) column to Excel export/import (2026-07-15 revision)

- [x] 11.1 In `packages/shared/src/index.ts`, add a `TRIP_STATUS_LABELS: Record<TripStatus, string>` map (`PLANNED: "Kế hoạch"`, `DISPATCHED: "Đã điều xe"`, `IN_TRANSIT: "Đang vận chuyển"`, `COMPLETED: "Hoàn thành"`, `CANCELLED: "Hủy"` — matching the web page's existing local `STATUS_LABELS` values exactly) and export it; the web page's own local constant is left as-is (not refactored to use this)
- [x] 11.2 In `apps/api/src/modules/export/builders/kehoach-xe.builder.ts`, insert a `"TRẠNG THÁI"` header (and matching `COL_WIDTHS` entry) into `HEADERS`, immediately after `"KHÁCH HÀNG"` and before `"LOẠI HÌNH"`; in the data row, insert `TRIP_STATUS_LABELS[tp.status] ?? ""` at the same position
- [x] 11.3 In `apps/api/src/modules/import/parsers/kehoach-xe.parser.ts`, add a `TRANG_THAI: { occurrence: 0, texts: ["TRẠNG THÁI"] }` entry to `COLUMN_CANDIDATES`, and a `status?: TripStatus` field to `ParsedTripPlanRow`
- [x] 11.4 In the same parser file, add a reverse-lookup helper (e.g. `reverseStatusLabel(text: string): TripStatus | undefined`) that compares `stripDiacritics(text)` against `stripDiacritics(TRIP_STATUS_LABELS[status])` for each `TripStatus` value; in the row-building object, set `status: reverseStatusLabel(cellText(row, COL.TRANG_THAI))`, and if the cell has non-empty text but no match, push a warning `` `Hàng ${rowNum}: không nhận dạng được trạng thái "${raw}"` `` to the `warnings` array (do not throw / do not skip the row)
- [x] 11.5 In `apps/api/src/modules/import/import.service.ts`'s `importTripPlans`, apply `row.status` on the UPDATE branch only (conditional-spread into `tripPlanData`, covered by the existing changed-record diff); do NOT add `status` to the CREATE branch's data object — new rows from import keep the schema default (`PLANNED`), consistent with `CreateTripPlanDto` having no `status` field
- [x] 11.6 `cd apps/api && pnpm exec tsc --noEmit` — confirm no type errors introduced
- [ ] 11.7 Manually export "kế hoạch xe" and confirm the TRẠNG THÁI column appears right after KHÁCH HÀNG with the correct Vietnamese label for each trip plan's status
- [ ] 11.8 Manually change a TRẠNG THÁI cell in an exported file (e.g. "Kế hoạch" → "Hoàn thành") and re-import, confirming the matching TripPlan's `status` is updated, the change appears in `changedRecords`, and a matching Audit Log entry is created
- [ ] 11.9 Manually create a brand-new row in an import file (no `id` cell) with a TRẠNG THÁI value of "Hoàn thành" and import it; confirm the created TripPlan has `status = "PLANNED"` (not "COMPLETED") — the CREATE branch ignores the parsed status
- [ ] 11.10 Manually put an unrecognized value (e.g. a typo) in an existing row's TRẠNG THÁI cell and re-import; confirm the import result's `warnings` includes a message for that row and the TripPlan's `status` is unchanged

## 12. Follow-up — inline status select in the trip plans list table (2026-07-15 revision)

- [x] 12.1 In `apps/web/src/app/(authenticated)/trip-plans/page.tsx`, add `handleStatusChange(id: string, status: TripStatus)` near `handleDelete`: optimistically update the row in local `trips` state, `PATCH /api/trip-plans/:id/status` with `{ status }`, show a success toast, and on failure revert `trips` to the previous value and show an error toast
- [x] 12.2 Replace the read-only status `<span>` badge in the list table's "Trạng thái" cell with a `<select>` (value = `trip.status`, `onChange` calls `handleStatusChange`), keeping the same per-status background/text color styling (`STATUS_COLORS`) and populating `<option>`s from `ALL_STATUSES`/`STATUS_LABELS`
- [x] 12.3 `cd apps/web && pnpm exec tsc --noEmit` — confirm no type errors introduced
- [x] 12.6 Polish the "Trạng thái" column visually: center the header (`textAlign: "center"` on the `<th>`) and the cell (`textAlign: "center"` on the `<td>`, `textAlignLast: "center"` on the `<select>` so the selected option text is centered too), and bump the select's font size/weight (11px/500 → 13px/600) and padding (2px 6px → 4px 10px) for legibility
- [ ] 12.4 Manually change a trip's status via the new inline select and confirm: the row updates immediately, `GET /trip-plans` (via page refresh) shows the persisted change, and an Audit Log entry ("STATUS_CHANGE") is created — requires running dev servers + browser, not exercised this session
- [ ] 12.5 Manually simulate a failed status update (e.g. stop the API server) and confirm the select reverts to the previous status and an error toast is shown — requires running dev servers + browser, not exercised this session
