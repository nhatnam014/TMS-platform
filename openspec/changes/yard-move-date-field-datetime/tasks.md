## 1. Database & Schema

- [x] 1.1 In `packages/db/prisma/schema.prisma`, change `YardMove.date` from `String` to `DateTime @db.Date` (remove the "free-text date entry" comment).
- [x] 1.2 Generate a Prisma migration for this change. Since existing `yard_moves` rows are test data with unparseable free-text dates, drop/truncate existing `yard_moves` rows as part of the migration rather than attempting a data cast.
- [x] 1.3 Run `prisma generate` and confirm the generated Prisma client types reflect `date: Date` on `YardMove`.

## 2. Backend DTOs & Service

- [x] 2.1 In `apps/api/src/modules/yard-move/dto/create-yard-move.dto.ts`, change `date` validation from `@IsString()` to `@IsDateString()`.
- [x] 2.2 In `apps/api/src/modules/yard-move/dto/update-yard-move.dto.ts`, change `date` validation from `@IsString()` to `@IsDateString()`.
- [x] 2.3 In `apps/api/src/modules/yard-move/yard-move.service.ts`, update the audit-log summary strings that interpolate `dto.date`/`existing.date` (create summary, delete summary) to format the date as `dd/mm/yyyy` instead of interpolating the raw value.

## 3. Excel Import (lệnh bãi)

- [x] 3.1 In `apps/api/src/modules/import/parsers/lenh-bai.parser.ts`, add a date-parsing helper: accept a native Excel date cell value (`Date` instance) directly, or parse `dd/mm/yyyy` text into a `Date`; return `undefined`/invalid marker for anything else. (Reused the existing shared `parseExcelDate` from `../utils/excel-date`, already used by `kehoach-xe.parser.ts` — handles native Date, Excel serial numbers, and `dd/mm/yyyy`/`dd-mm-yyyy`/ISO text. **Bug found + fixed during verification**: `parseExcelDate` builds dates with the local-timezone `new Date(y, m, d)` constructor, which shifts the stored value back a day in positive-UTC-offset zones — this server runs in `Asia/Saigon` (UTC+7), so every imported date landed one day early once serialized to `@db.Date`. Added a local `toUtcDateOnly()` re-anchor in `lenh-bai.parser.ts` after calling `parseExcelDate`, scoped to this file only — did not touch the shared utility itself, since that's used by `kehoach-xe.parser.ts`/trip-plan import too and fixing it there is out of this change's scope. The `spx-impl-verifier` subagent flagged that applying this re-anchor unconditionally could reintroduce a shift in the opposite direction for native Excel-date-typed cells in negative-UTC-offset deployments — fixed by only applying `toUtcDateOnly` when the raw cell value is a string; native `Date`/numeric-serial values from `parseExcelDate` are already correctly UTC-anchored and now pass through unchanged. Re-verified live against both a text-date row and a native-Excel-date-cell row — both round-trip with zero drift.)
- [x] 3.2 Update `parseLenhBai` to use the new date-parsing helper for the `NGÀY` column instead of `cellText()`. Keep the existing "missing NGÀY" error path for empty cells.
- [x] 3.3 Add a new error path: when `NGÀY` is non-empty but fails to parse as a date, push a row error (distinct message from "thiếu ngày") and skip the row, matching the existing error-row convention.
- [x] 3.4 Update the `ParsedYardMoveRow.date` type from `string` to `Date` (or `string | Date` if an ISO string is preferred for the intermediate representation — confirm consistency with how `TripPlan` import passes dates to Prisma) and update `import.service.ts#importYardMoves` (`data.date = row.date!`) accordingly so it's passed to Prisma as a valid `Date`/ISO value.

## 4. Excel Export (lệnh bãi)

- [x] 4.1 In `apps/api/src/modules/export/builders/lenh-bai.builder.ts`, change the NGÀY cell write from `rec.date ?? ""` (raw pass-through) to `formatDate(rec.date)`, reusing the file's existing local `formatDate()` helper — matches the `dd/mm/yyyy`-string convention used by `kehoach-xe`/`quanly-xe`/`baoduong-xe` builders (design.md was corrected during implementation: no native-Date-cell/`numFmt` approach exists anywhere else in this codebase's export builders, so that original plan was dropped in favor of the actually-consistent pattern).
- [x] 4.2 Verify the STT/ID/other columns are unaffected by the cell-writing change (row indices, borders, greyed-out ID styling still apply correctly).

## 5. Frontend Form & List

- [x] 5.1 In `apps/web/src/app/(authenticated)/yard-moves/page.tsx`, replace the NGÀY `<input type="text" placeholder="24/06">` in `YardMoveFormFields` with `<input type="date">`.
- [x] 5.2 Update the create-modal date state initialization and the edit-modal pre-fill (`move.date ?? ""`) to use `toDateInput` from `@/lib/date-utils` so the value is in `yyyy-mm-dd` form for the date input. (Create modal's `EMPTY_FORM` already starts at `date: ""`, which `toDateInput` would also produce for empty input, so only the edit-modal pre-fill needed the explicit `toDateInput(move.date)` call.)
- [x] 5.3 Update the submit handlers (create and edit) to send the date input's `yyyy-mm-dd` value as the `date` field — confirm this is accepted as-is by `@IsDateString()` on the backend (it is, since `yyyy-mm-dd` is a valid ISO date string). (No code change needed — both handlers already send `date: values.date` verbatim.)
- [x] 5.4 Update the table's NGÀY cell (`{m.date || "—"}`) to render via `formatDate` from `@/lib/date-utils` instead of the raw value.
- [x] 5.5 Update the `ariaLabel` on `SelectionCheckbox` (`Chọn tiến độ vận tải ${m.date}`) to use the formatted date for readability.

## 6. Verification

- [x] 6.1 Run API and web type-checks/builds and fix any compile errors surfaced by the schema/DTO/type changes. (`@tms/api` and `@tms/web` type-check and build cleanly. `@tms/db`/`@tms/shared` `type-check` scripts fail, but confirmed via `git stash` that the failure is pre-existing and unrelated to this change — a repo-wide tsconfig issue pulling in `apps/web`/`packages/ui` JSX files.)
- [x] 6.2 Manually verify in the browser: create a yard move with the new date picker, edit an existing one, confirm the list displays `dd/mm/yyyy`. (Verified end-to-end against the live API with curl: create/update/soft-delete round-trip correctly, invalid `date` values are rejected with 400, audit summaries read cleanly. Verified `/yard-moves` renders HTTP 200 with no server error via the running Next dev server. Did **not** visually confirm the native date-picker widget or click through the modal in an actual browser — no browser-automation tool was available in this environment; the `type="text"` → `type="date"` swap itself is a minimal, low-risk change reusing the same `toDateInput`/`formatDate` helpers `TripPlan.tripDate` already uses successfully.)
- [x] 6.3 Manually verify Excel export: download `lenh-bai.xlsx`, confirm the NGÀY column renders as a real Excel date (right-aligned, date-formatted) rather than left-aligned text. (Design corrected during implementation — see task 4.1 note: the codebase's actual, consistent convention across all 4 export builders is a `dd/mm/yyyy` formatted string cell, not a native date-typed cell. Verified via a real export download + ExcelJS read-back: NGÀY cell contains `"24/06/2026"` for a `2026-06-24` record.)
- [x] 6.4 Manually verify Excel import round-trip: re-import the exported file, confirm dates are read back correctly with no errors; also test importing a hand-edited file with a `dd/mm/yyyy` text date and one with an invalid date value (confirm it's reported as a row error, not silently accepted or crashing). (Verified against the live API: export → re-import round-trip preserves the exact date with zero drift (after fixing a timezone bug found during this test — see below); a crafted file with an unparseable NGÀY value produced a distinct `"ngày ... không đúng định dạng"` error, a missing NGÀY produced `"thiếu ngày"`, and a fully empty row was silently skipped with no error — all three behave exactly as specced.)
- [x] 6.5 Run existing automated test suites (API + web) and update any tests still asserting `date` as a free-text string. (No `.spec.ts`/`.test.ts`/`.test.tsx` files exist anywhere in `apps/api` or `apps/web` in this repo — nothing to run or update.)
